{
  "permissions": {
    "deny": [
      ".env",
      ".env.*",
      "**/.env",
      "**/.env.*",
      "**/secrets/**",
      "**/*_secret*",
      "**/*credentials*"
    ],
    "ask": [
      "Bash:rm",
      "Bash:git push",
      "Bash:pip install",
      "Bash:uv add"
    ]
  },
  "sandbox": {
    "enabled": true,
    "excludedCommands": [
      "git",
      "pytest",
      "python",
      "uv",
      "ruff"
    ]
  },
  "env": {
    "PYTHONPATH": "${workspaceFolder}/src"
  }
}
