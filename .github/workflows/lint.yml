name: Lint

on:
  push:
    branches: [main]
  pull_request:
    # feat/m1-gold-set: temporary M1 integration branch so per-wave PRs into it get
    # full CI. REVERT this line in the integration->main PR.
    branches: [main, feat/m1-gold-set]

jobs:
  lint:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.11.24"
          enable-cache: true

      - name: Install dependencies
        run: uv sync

      - name: Run ruff check
        run: uv run ruff check .

      - name: Run ruff format check
        run: uv run ruff format --check .

      - name: Run mypy
        run: uv run mypy src
