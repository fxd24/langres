# Do instruction prompts help embedding blockers? Per model, per benchmark

Generated by `examples/research/prompt_axis.py`. Every number is **candidate recall** at a fixed `k` -- no threshold anywhere, so nothing here confounds the retrieval effect with a cut. Confidence intervals are paired bootstraps resampled **by gold cluster** (`langres.experiments.statistics.paired_entity_bootstrap`), never by pair row.

**Results are reported per model. They are deliberately not averaged across 
models -- the whole question is whether the effect is model-specific.**

## Where each prompt came from

| model | licence | source of its documented prompt |
|---|---|---|
| `sentence-transformers/all-MiniLM-L6-v2` | apache-2.0 | config_sentence_transformers.json (snapshot c9745ed1d9f207416be6d2e6f8de32d1f16199bf) registers no usable prompts. Not instruction-trained -- included as the CONTROL: whatever a generic instruction does here is what it does to a model that was never taught to read one. |
| `BAAI/bge-base-en-v1.5` | mit | model card README.md (snapshot a5beb1e3e68b9ab74eb54cfd186867f64f240e1a), Model List table L2679 gives the query instruction verbatim; note [1] L2692: 'In all cases, no instruction needs to be added to passages'; L2738-2744: v1.5 was tuned to work WITHOUT the instruction and 'the best method to decide whether to add instructions for queries is choosing the setting that achieves better performance on your task'. Its config_sentence_transformers.json registers no prompts at all. |
| `intfloat/e5-base-v2` | mit | model card README.md (snapshot f52bf8ec8c7124536f0efb74aca902b2995e5bcd), L2631 'Each input text should start with "query: " or "passage: "' and FAQ 1 L2690-2694: 'query:'/'passage:' for ASYMMETRIC tasks, 'query:' on both sides for SYMMETRIC tasks (semantic similarity, paraphrase retrieval) and for clustering. The checkpoint ships NO config_sentence_transformers.json, so the card is the only source. |
| `google/embeddinggemma-300m` | gemma (NOT OSI-approved; use-restricted) | config_sentence_transformers.json (snapshot 57c266a740f537b4dc058e1b0cda161fd15afa75) 'prompts' mapping, cross-checked against the model card's 'Prompt Instructions' section L344 + task table L366-416. Query form is 'task: {task description} | query: ', document form is 'title: {title | "none"} | text: '. The card lists a Semantic Similarity template ('task: sentence similarity | query: ', flagged 'not intended for retrieval use cases') and a Clustering template. |
| `Qwen/Qwen3-Embedding-0.6B` | apache-2.0 | config_sentence_transformers.json (snapshot c54f2e6e80b2d7b7de06f51cec4959f6b3e03418): prompts = {'query': 'Instruct: Given a web search query, retrieve relevant passages that answer the query\nQuery:', 'document': ''} -- the document prompt is literally empty. Model card L129 gives the template f'Instruct: {task_description}\nQuery:{query}', L138 'No need to add instruction for retrieval documents', and L54 'using instructions typically yields an improvement of 1% to 5% ... we recommend that developers create tailored instructions specific to their tasks'. |

## The arms

| model | arm | kind | document side | query side | note |
|---|---|---|---|---|---|
| `sentence-transformers/all-MiniLM-L6-v2` | `none` | baseline | - | - | No prompt on either side. |
| `sentence-transformers/all-MiniLM-L6-v2` | `er_symmetric` | ours | `'Find records that describe the same real-world entity as: '` | `'Find records that describe the same real-world entity as: '` | Our ER instruction on BOTH sides -- coherent, since ER blocking is a symmetric record-to-record task. |
| `sentence-transformers/all-MiniLM-L6-v2` | `er_query_only` | trap | - | `'Find records that describe the same real-world entity as: '` | Our ER instruction on the QUERY side only, documents bare. The half-driven recipe #242 warns about; measured here rather than assumed. |
| `BAAI/bge-base-en-v1.5` | `none` | baseline | - | - | No prompt on either side. |
| `BAAI/bge-base-en-v1.5` | `er_symmetric` | ours | `'Find records that describe the same real-world entity as: '` | `'Find records that describe the same real-world entity as: '` | Our ER instruction on BOTH sides -- coherent, since ER blocking is a symmetric record-to-record task. |
| `BAAI/bge-base-en-v1.5` | `er_query_only` | trap | - | `'Find records that describe the same real-world entity as: '` | Our ER instruction on the QUERY side only, documents bare. The half-driven recipe #242 warns about; measured here rather than assumed. |
| `BAAI/bge-base-en-v1.5` | `official_query_instruction` | documented | - | `'Represent this sentence for searching relevant passages: '` | The card's own recipe. Query-side only is DOCUMENTED here: 'In all cases, no instruction needs to be added to passages.' |
| `BAAI/bge-base-en-v1.5` | `official_symmetric` | ours | `'Represent this sentence for searching relevant passages: '` | `'Represent this sentence for searching relevant passages: '` | The official instruction made symmetric -- NOT documented. Tests whether a symmetric task wants a symmetric prefix. |
| `intfloat/e5-base-v2` | `none` | baseline | - | - | No prompt on either side. |
| `intfloat/e5-base-v2` | `er_symmetric` | ours | `'Find records that describe the same real-world entity as: '` | `'Find records that describe the same real-world entity as: '` | Our ER instruction on BOTH sides -- coherent, since ER blocking is a symmetric record-to-record task. |
| `intfloat/e5-base-v2` | `er_query_only` | trap | - | `'Find records that describe the same real-world entity as: '` | Our ER instruction on the QUERY side only, documents bare. The half-driven recipe #242 warns about; measured here rather than assumed. |
| `intfloat/e5-base-v2` | `official_asymmetric` | documented | `'passage: '` | `'query: '` | The card's retrieval recipe: 'query:'/'passage:' for asymmetric tasks. |
| `intfloat/e5-base-v2` | `official_symmetric` | documented | `'query: '` | `'query: '` | The card's SYMMETRIC recipe -- 'use query: prefix for symmetric tasks'. ER blocking is symmetric, so this is the documented recipe for this use, not the retrieval one. |
| `google/embeddinggemma-300m` | `none` | baseline | - | - | No prompt on either side. |
| `google/embeddinggemma-300m` | `er_symmetric` | ours | `'Find records that describe the same real-world entity as: '` | `'Find records that describe the same real-world entity as: '` | Our ER instruction on BOTH sides -- coherent, since ER blocking is a symmetric record-to-record task. |
| `google/embeddinggemma-300m` | `er_query_only` | trap | - | `'Find records that describe the same real-world entity as: '` | Our ER instruction on the QUERY side only, documents bare. The half-driven recipe #242 warns about; measured here rather than assumed. |
| `google/embeddinggemma-300m` | `official_retrieval` | documented | `'title: none | text: '` | `'task: search result | query: '` | The config's Retrieval-document / Retrieval-query pair. |
| `google/embeddinggemma-300m` | `official_sts` | documented | `'task: sentence similarity | query: '` | `'task: sentence similarity | query: '` | The config's STS / PairClassification template, applied to both sides as a symmetric template must be. |
| `google/embeddinggemma-300m` | `official_clustering` | documented | `'task: clustering | query: '` | `'task: clustering | query: '` | The config's Clustering template, both sides. |
| `google/embeddinggemma-300m` | `er_in_official_template` | ours | `'task: entity resolution | query: '` | `'task: entity resolution | query: '` | An ER task description dropped into the card's OWN template shape. Tests whether the template or the task word is what matters. |
| `Qwen/Qwen3-Embedding-0.6B` | `none` | baseline | - | - | No prompt on either side. |
| `Qwen/Qwen3-Embedding-0.6B` | `er_symmetric` | ours | `'Find records that describe the same real-world entity as: '` | `'Find records that describe the same real-world entity as: '` | Our ER instruction on BOTH sides -- coherent, since ER blocking is a symmetric record-to-record task. |
| `Qwen/Qwen3-Embedding-0.6B` | `er_query_only` | trap | - | `'Find records that describe the same real-world entity as: '` | Our ER instruction on the QUERY side only, documents bare. The half-driven recipe #242 warns about; measured here rather than assumed. |
| `Qwen/Qwen3-Embedding-0.6B` | `official_query_instruct` | documented | - | `'Instruct: Given a web search query, retrieve relevant passages that answer the query\nQuery:'` | The config's registered 'query' prompt verbatim; its 'document' prompt is the empty string, so a bare document side IS the documented recipe. Note the task text is about WEB SEARCH, so this arm measures the card's default applied outside its stated domain. |
| `Qwen/Qwen3-Embedding-0.6B` | `er_in_official_template` | ours | - | `'Instruct: Given a product or business record, retrieve other records that describe the same real-world entity\nQuery:'` | The card's own template with an ER task description substituted -- exactly what the card tells developers to do ('create tailored instructions specific to your tasks'). Document side stays bare, per the card. |

## Effect on candidate recall at k=20

`doc shift` / `query shift` are `1 - mean cosine` against the no-prompt arm's vectors: **any non-zero value is proof the prompt reached the encoder**, and a prompted arm measuring exactly 0 aborts the run instead of reporting a flat result. `pair J` is the Jaccard overlap of the candidate-pair set with the no-prompt arm -- proof the changed vectors changed the *retrieved neighbours*, not just the geometry. `**` on an interval means it excludes zero.

### `sentence-transformers/all-MiniLM-L6-v2`

22713216 params, dim 384, licence `apache-2.0` (OSI-approved).

Control: not instruction-trained, ships no prompts.

| benchmark | arm | kind | recall | Δ vs none | 95% CI | doc shift | query shift | doc·query | pair J | candidates |
|---|---|---|---:|---:|---|---:|---:|---:|---:|---:|
| fodors_zagat | `none` | baseline | 1.0000 | 0.0000 | [+0.0000, +0.0000] (spans 0) | 0 | 0 | 1.0000 | 1.000 | 4880 |
| fodors_zagat | `er_query_only` | trap | 1.0000 | 0.0000 | [+0.0000, +0.0000] (spans 0) | 0 | 0.2486 | 0.7514 | 0.459 | 5294 |
| fodors_zagat | `er_symmetric` | ours | 1.0000 | 0.0000 | [+0.0000, +0.0000] (spans 0) | 0.2486 | 0.2486 | 1.0000 | 0.524 | 4936 |

## Every k

| model | benchmark | arm | k | recall | recall/reachable | candidates |
|---|---|---|---:|---:|---:|---:|
| `sentence-transformers/all-MiniLM-L6-v2` | fodors_zagat | `er_query_only` | 20 | 1.0000 | 1.0000 | 5294 |
| `sentence-transformers/all-MiniLM-L6-v2` | fodors_zagat | `er_symmetric` | 20 | 1.0000 | 1.0000 | 4936 |
| `sentence-transformers/all-MiniLM-L6-v2` | fodors_zagat | `none` | 20 | 1.0000 | 1.0000 | 4880 |
